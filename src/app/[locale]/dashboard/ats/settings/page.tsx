import { AtsSettingsClient } from "@/components/ats/management/AtsSettingsClient";

/* ATS-M1 — the organization's ATS settings (workflow, AI review, retention, notifications, public careers, security). */
export default function AtsSettingsPage() {
  return <AtsSettingsClient />;
}
